{
  "name": "riak-bucket-cleanup",
  "version": "0.0.1",
  "description": "Small node utility to export a whole bucket with all keys from riak into a file",
  "main": "./bin/riak-bucket-cleanup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "riak",
    "cleanup",
    "regex",
    "bucket"
  ],
  "bin": {
    "riak-bucket-cleanup": "./bin/riak-bucket-cleanup.js"
  },
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/maurermax/riak-bucket-cleanup.git"
  },
  "author": "Max Maurer <maxemanuel.maurer@gmail.com> (http://max-maurer.de/)",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.3.0",
    "riak-js": "^0.10.2"
  }
}
